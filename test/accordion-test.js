const assert = require('assert');

describe('Accordion', () => {
    it('should open a panel when a user click on a panel header', () => {
        console.log(window);


        assert.equal(1, 1);
    });
});